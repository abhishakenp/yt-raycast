export function initRocketExhaust() {
  // Intentionally lightweight fallback for environments where the optional rocket
  // exhaust animation bundle is unavailable. Kept as a no-op to avoid homepage
  // runtime errors and preserve prompt/autocomplete/interactions.
}
