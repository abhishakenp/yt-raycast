# API reference overlay

- Prefer `docs-content` sections with grouped `items` (method, path, summary).
- Include authentication and error-handling topics when prompt implies public API.
- `contentGoals` per page: discoverability, copy-paste examples, rate limits (only if stated in prompt).
