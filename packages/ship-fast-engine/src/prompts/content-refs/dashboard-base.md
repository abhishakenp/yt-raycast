# Dashboard / app shell

## Pages

- Home (overview), primary workflow pages from prompt (e.g. Reports, Settings, Team).

## Sections

- Use `dashboard-shell` for main app canvas; pair with `stats`, `features` (as widgets), `team` if relevant.
- `layoutType` `app-shell` on primary app routes.

## Navigation

- Global nav reflects modules; avoid marketing-only fluff.
