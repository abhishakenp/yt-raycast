# Portfolio

## Pages

- Home, Work/Projects, About, Contact (or prompt-specific).

## Homepage

- Strong `hero` or gallery-forward layout; `features` or `gallery` for case studies; `contact-form` or `cta`.

## Content

- `pageRole` on work index: `showcase`; project pages: `case-study` with `contentGoals` (problem, approach, outcome).
