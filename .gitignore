node_modules
coverage
.idea/
.DS_Store
dist
dist-ssr
*.local
.env
# `*.local` does NOT cover these — a plain-text secret file one `git add .`
# away from permanent history.
.env.*.bak
.env.local.bak
.env.*.local
.nitro
.tanstack
.wrangler
.output
.quality-review-groups
.vinxi
__unconfig*
todos.json

.agents/skills/
scripts/.lakebed-logs/
scripts/.lakebed-conversion-report.json
/.forge
/.playwright-mcp
.env.prod

# Local subdomain proxy certs (generated by traefik:setup)
.local-proxy/certs/

# Generated files — regenerate via scripts, never commit
packages/ship-fast-blocks/src/generated/
tsconfig.tsbuildinfo

# Rust build artifacts
wasm-particles/target/

# Local scratch / tooling output that must never be committed
output.txt
.interview/
node_modules/.cache/ship-fast-svelte-ssr/
specs/architecture/dev-staging-setup.md
