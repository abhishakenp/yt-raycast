/** Canonical OpenUI source filename in a session workspace. */
export const HOME_OPENUI_FILE = 'home.openui'

/** Directory for generated non-home OpenUI page artifacts in a session workspace. */
export const OPENUI_PAGES_DIR = 'pages'

/** Manifest mapping site routes to generated OpenUI source files. */
export const OPENUI_MANIFEST_FILE = 'openui-manifest.json'
