# Blog / editorial

## Pages
- Home, Blog index, Post template (as needed), About, Newsletter/Contact.

## Sections
- `blog-list` on index with rich `items` (title, excerpt, date, href).
- Article pages: `hero` + `docs-content` or stacked `features` for body chunks; `contentBlocks` for long prose.
